import Ionicons from "@expo/vector-icons/Ionicons";

export const Gear = ({ color, size }: { color: string; size: number }) => {
  return <Ionicons name="settings-sharp" size={size} color={color} />;
};
